db {
  profile = "slick.jdbc.MySQLProfile$"
  db {
    url = "jdbc:mysql://localhost:1111/your_database_name?useSSL=false&serverTimezone=UTC"
    driver = "com.mysql.cj.jdbc.Driver"
    connectionPool = "disabled"
    user = "root"
    password = "password"
  }
}
